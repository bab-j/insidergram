<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="feedDAO">
	
	<select id="getFollowingIdList" resultType="follower" parameterType="user">
		SELECT TO_ID
		  FROM FOLLOWER
		 WHERE FROM_ID = #{u_id}
	</select>
	
	<select id="getFollowingFeedList" resultType="feed" parameterType="follower">
		SELECT F.F_IDX, F.REGDATE, F.CONTENT, F.U_ID, F.F_PIC, U.U_PIC
		  FROM FEED F JOIN USER_INFO U
		    ON F.U_ID = U.U_ID
		 WHERE F.U_ID = #{to_id}
	</select>
	
  	
</mapper>







