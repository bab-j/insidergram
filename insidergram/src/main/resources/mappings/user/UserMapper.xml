<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="userDAO">

	<select id="getUser" parameterType="com.spring.biz.user.UserVO"
		resultType="user">
		SELECT * FROM USER_INFO WHERE U_ID = #{u_id} AND PASSWORD =
		#{password}
	</select>

	<!--아이디 중복체크 -->
	<select id="confirmID" resultType="int" parameterType="user">
		SELECT COUNT(U_ID)
		FROM USER_INFO
		WHERE U_ID = #{u_id}
	</select>

	<insert id="insertUser" parameterType="user">
		INSERT INTO USER_INFO
		(U_ID, PASSWORD, PHONE, EMAIL, BIRTH, NAME)
		VALUES (#{u_id}, #{password}, #{phone}, #{email}, #{birth}, #{name})
	</insert>
	
	<select id="findId" resultType="user" parameterType="user">
		SELECT COUNT(U_ID)
		FROM USER_INFO
		WHERE U_ID = #{u_id}
	</select>
</mapper>







